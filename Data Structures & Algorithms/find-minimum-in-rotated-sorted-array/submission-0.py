class Solution:
    def findMin(self, nums: List[int]) -> int:
        # left = 0
        # right = len(nums)-1
        # mid = (left + right)//2
        # while left < right:
        #     if nums[mid] ==
        return min(nums) 